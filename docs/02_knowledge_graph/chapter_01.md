##### 问题一：
复杂套餐如何应用于知识图谱的？
如果 客户开通了 **A**，且在 **X 地区**,  那么 **套餐 B 打 8 折**             **规则一**
如果 客户是政企大客户,  那么 **套餐 B 打 7 折**                           **规则二**
如果 客户开通了 **C** ， 那么 **不能享受任何关于 B 的折扣**            **规则三**

```
客户 K ——开通——> 套餐 A
客户 K ——所在地区——> 地区 X
```

```
规则 R1 ——需要——> 套餐 A
规则 R1 ——需要——> 地区 X
```

```
规则 R1 ——作用于——> 套餐 B
```

```
规则 R2 ——作用于——> 套餐 B
```

```
规则 R3 ——需要——> 套餐 C
规则 R3 ——否定——> 套餐 B
```

**在知识图谱中，规则不是关系，而是节点；条件是规则指向的事实；知识图谱的主要作用是基于图路径（Path）的计费/规则关联解释（Explainability）**

{
  "evidence_path": [
    {"node": "A套餐", "relation": "包含"},
    {"node": "B流量包", "relation": "影响"},
    {"node": "C资费规则"}
  ]
}





##### 问题二：
**你的图谱构建 准确率（Precision） 和 完备率（Recall） 是如何测算的？ 基准测试： 针对账务系统，有没有构建专门的 抽检集？对于“非法关系”或“过时规则”，你们有没有定期的 知识清理（KG Cleaning） 机制？**

对知识图谱构建采用信息抽取评估体系，通过 Precision / Recall 量化质量；  
构建了覆盖高风险条款的 Golden Triples 作为基准测试；  
同时引入 Schema 校验、规则冲突检测、时间失效与置信度衰减机制，形成持续的 KG Cleaning 闭环，确保账务规则长期可用、可审计。




**问题三：**
**针对文档修改后，向量数据库与知识图谱是如何完成同步的，对于删除的套餐规则，知识图谱是怎么处理的？**
文档修改通过结构化 Diff 驱动向量库与知识图谱的增量同步；  
所有规则节点都绑定文档版本与条款来源；  
对于删除或替换的套餐规则，采用软删除（失效标记）而非物理删除，  
保证账务规则可追溯、可复算、可审计。




**问题四：**
**知识图谱提取的实体有哪些？**


- 客户与账户实体（Who）
- 产品与套餐实体（What）
- 计费与规则实体（How）
- 账务与用量实体（Result）
- 时间与条件实体（When / Under what）
- 语言与映射实体（How users say it）

```
Customer
  └─ owns → Account
        └─ has → Subscription
              └─ uses → TariffPlan
                    └─ includes → Addon
                          └─ affects → ChargingRule
                                └─ generates → BillingItem
                                      └─ part_of → Bill
```



**问题五：** 
深挖提问： “当业务系统中一个套餐的资费发生了变更，你的 RAG 索引和知识图谱是如何 ‘分钟级同步’ 的？你如何解决 ‘索引重建期间的旧数据残留’ 问题？是否实现了一套 增量索引（Incremental Indexing） 机制？如果模型在微调中已经‘背下了’旧规则，你如何通过 知识解耦（Knowledge Unlearning） 或 强 Prompt 覆盖 来修正它的记忆？

假设今天发生一件事：早上10:00，运营商决定：20G套餐从199降到179。
第一步：修改计费系统，将套餐数据从199修改为179
第二步：修改知识图谱，只修改一条节点的数据
第三步：RAG向量数据库更新，删除旧向量，生成新文档，重新生成向量，插入向量数据库。。（2mins）
第四步：构建新旧索引，新索引构建好之前，不影响旧索引，构建完成后瞬间切换。
第五步：防止模型说出旧的套餐价格，通过写死prompt，资费问题以检索结果为准，禁止使用模型记忆。